using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Alien : MonoBehaviour
{
    // Start is called before the first frame update

    private bool tienePistola;

    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        /*Aqui va el if para chequear si la pistola aun esta y cambias el bool a false si no existe su pistola*/



        if (!tienePistola) { 
        /*ejecutar animacion de alien bailando*/
        //triggerAnim(etc)

        
        }


    }
}
